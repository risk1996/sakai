[package]
name = "sakai-core"
version = "0.1.0"
edition = "2024"

[dependencies]
nutype = "0.7.0"
thiserror = "2.0.20"
uom = { version = "0.38.0", default-features = false, features = ["u64", "si"] }
